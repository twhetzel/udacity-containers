# Hello World Exercise

## Build the Docker image as:
- `docker build --tag test .`


## Run the image as:
- `docker run test -rm`
NOTE: Adding `-rm` removes the container when it is done running

The output "hello world" will be displayed on the terminal.


